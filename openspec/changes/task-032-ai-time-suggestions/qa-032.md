# QA Report — TASK-032 AI time-entry suggestions

- **Gate**: SW-3 (full A+B+C branch)
- **Branch**: `feat/task-032-ai-time-suggestions` · PR #27
- **Tier**: L2
- **Verdict**: **PASS**

---

## Recheck — 2026-06-20 (post SW-4 fix loop 1)

Full SW-3 re-run on HEAD `dc60924` after SW-4 blockers were remediated:

- **B1 fixed**: `generate()` wraps delete + inserts + list in `unchecked_transaction()`; new test
  `failed_regeneration_preserves_the_original_pending_set` proves the original pending set survives an
  insert-aborting trigger. Rust suggestions 13/13 PASS (was 12).
- **B2 fixed**: stale `#![allow(dead_code, unused_imports)]` removed from `suggestions/mod.rs`; dead
  `EvidenceRow.trace_id` field/projection removed from `store.rs` (column kept in ORDER BY only).
  `cargo clippy --all-targets` → 0 warnings in `suggestions/`; pre-existing langfuse/ warnings unchanged.
- All other A+B+C checks re-run and PASS (see table below).

---

## Test suite results

| Suite | Run | Pass | Fail | Notes |
|---|---|---|---|---|
| Rust (`cargo test --lib`) | 159 | 159 | 0 | 13 suggestions tests (+1 new failure-path test) |
| Suggestions builder suite | 10 | 10 | 0 | all TASK-032 C assertions |
| `npm run build` (tsc strict + vite) | — | ✓ | — | no errors |
| `cargo clippy --all-targets` | — | ✓ | — | 0 warnings in suggestions/; pre-existing langfuse/ warnings outside scope |
| `cargo fmt --all -- --check` | — | ✓ | — | clean |
| `git diff --check HEAD` | — | ✓ | — | no trailing whitespace |

---

## Scenario coverage matrix

### Workstream A — Engine + persistence

| Scenario | Spec ref | Implementation | Test | Result |
|---|---|---|---|---|
| `time_entry_suggestions` table created idempotently | design §1.1 | `suggestions::store::migrate` — correct schema, UNIQUE key, index | Rust seeded-fixture tests | PASS |
| Clustering by 30-min GAP constant | design §2.2 | `engine.rs cluster_by_gap`: gap checked in seconds; single constant `GAP_MINUTES=30` | `suggestions/tests.rs` | PASS |
| Timed block: span = min(start)..max(end); duration = max(1, round) | design §2.3 | `timed_block` in `engine.rs` | Rust A-checkpoint tests | PASS |
| **Absence ≠ zero** — untimed bucket → `duration_minutes=NULL` never 0 | DEC-004 | `untimed_block` sets all Option fields to `None` | `tests/suggestionsUi.test.mjs`: `durationLabel` + `untimed row` tests | PASS |
| **Absence ≠ zero** — unknown tokens/cost → `None` not 0 | DEC-004 | `Aggregate::of` preserves `None` throughout | Builder tests: `tokens/cost unknown` | PASS |
| Unmapped envs → separate summary, never dropped | DEC-004 | `unmapped_summary` tallied independently; never folded into block | `unmappedNotice` builder test | PASS |
| Mixed-currency cost → `cost_total=None`, noted in reason | design §2.2 | `currencies.len() > 1` branch in `Aggregate::of` | Rust A tests | PASS |
| Idempotent generate: accepted/dismissed preserved | design §2 | `insert_if_not_decided` with explicit `IS NULL` guard for untimed natural-key dedup | Rust A tests | PASS |
| **Atomic replace-set**: failed regeneration leaves original pending set intact | design §2 (SW-4 B1) | `unchecked_transaction` wraps delete + inserts + list; dropped without commit on insert error → rollback | `failed_regeneration_preserves_the_original_pending_set` (new) | PASS |
| **No dead code / no hidden lint suppression** | (SW-4 B2) | `mod.rs` has no `allow(dead_code,unused_imports)`; `EvidenceRow.trace_id` removed from struct + projection | `cargo clippy --all-targets` 0 warnings in suggestions/ | PASS |
| **No auto-posting** during generate | DEC-006 | `engine::generate` writes only `time_entry_suggestions`, never `time_entries` | Rust A tests confirm 0 `time_entries` writes | PASS |
| **No secrets** in suggestion fields | SEC-012 | `Suggestion` carries env name, project ref, local time, aggregate numbers, health/confidence — no session id, payload, credential | SEC-012 builder test | PASS |
| **No egress** | DEC-001/017 | `engine.rs` comment + implementation: reads SQLite only | Code inspection | PASS |

### Workstream B — Accept / dismiss IPC + reporting

| Scenario | Spec ref | Implementation | Test | Result |
|---|---|---|---|---|
| `time_entries.origin` column added idempotently | design §1.2 | `add_column_if_absent` (already `pub(crate)`); `DEFAULT 'manual'` backfills existing rows | Rust B test `summary_and_csv` | PASS |
| `list_time_entry_suggestions(regenerate=false)` → current pending, fresh unmapped | design §3 | `suggestions::current()` — no delete/insert; computes unmapped from evidence join | Rust B + code inspection | PASS |
| `list_time_entry_suggestions(regenerate=true)` → fresh pending set | design §3 | `suggestions::generate()` | Rust B tests | PASS |
| **Accept timed**: single tx, creates one `origin='ai_suggested'` entry, marks `accepted` | DEC-006, design §3 | `accept_suggestion_repo` with guarded UPDATE `WHERE status='pending'` | B test `accept_creates_exactly_one_ai_entry` | PASS |
| **Accept untimed**: requires edits with start/end, never invents duration | DEC-004, design §3 | `let (Some(start_time), Some(end_time)) = …` guard → explicit error | B test `accept_of_untimed_block_requires_edits` | PASS |
| **No re-deciding**: accept-of-accepted/dismissed → error | design §3 | Status match in `accept_suggestion_repo` | B test (re-accept and dismiss both rejected) | PASS |
| **Dismiss**: writes no entry, idempotent, rejects dismiss-of-accepted | design §3 | `dismiss_suggestion_repo` — `"dismissed"` arm returns `Ok(())` | B test `dismiss_writes_no_entry_is_idempotent` | PASS |
| **AI ≠ human** — `summary_repo` separates origins | DEC-003 | CASE WHEN `origin='ai_suggested'` → `ai_minutes`; else `duration_minutes` | B test `summary_and_csv_report_human_and_ai_time_separately` | PASS |
| CSV carries `origin` column per row | DEC-003 | Header: `…note,origin,total_duration_hours`; per-row `csv_escape(&e.origin)` | B test (CSV header + `,manual,` / `,ai_suggested,` rows) | PASS |
| **No secrets** in provenance note | SEC-012 | `resolve_ai_note` uses `source` + `reason` only (both already secret-free) | Code inspection | PASS |

### Workstream C — Review/Accept UI

| Scenario | Spec ref | Implementation | Test | Result |
|---|---|---|---|---|
| `'Suggestions'` view in `View` type + `views` array | design §4, C1 | `main.ts:13,24` | Code inspection | PASS |
| `renderSuggestions()` loads with `regenerate=false` | design §4, C1 | `main.ts:63` | Code inspection | PASS |
| Grouped list by project → date, sorted | design §4, C2 | `suggestionGroups()` | Builder test `suggestions are grouped by project` | PASS |
| Timed row: Accept + Edit + Dismiss; edit panel hidden | design §4, C2 | `suggestionRow` (timed path) | Builder test `timed row` | PASS |
| **Untimed row**: no plain Accept; edit panel visible; start/end required | DEC-004, DEC-006, C2 | `suggestionRow` (untimed path) | Builder test `untimed row` | PASS |
| Edit toggles inline panel; `Accept with edits` submits `edits` | design §4, C2 | `bindSuggestions` data-edit handlers + `editsFor` | Code inspection | PASS |
| **Refresh** button → `regenerate=true` then re-render | design §4, C3 | `bindSuggestions` `#refreshSuggestions` handler | Code inspection | PASS |
| Unmapped notice → env + count + Settings link | design §4, C3 | `unmappedNotice()` + `data-goto-view="Settings"` | Builder test `unmapped evidence surfaced` | PASS |
| **Empty state**: names candidate causes, never bare table | design §4, C3 | `emptyState(hasUnmapped)` | Builder test `empty state names causes` | PASS |
| **AI ≠ human** — `summaryCards()` keeps human total headline; AI as `<small>` sub-line | DEC-003 | `main.ts:53` `summaryCards` | Code inspection | PASS |
| AI sub-line absent when `ai_minutes=0` | DEC-003 | `ai>0?…:''` guard in `summaryCards` | Code inspection | PASS |
| **Secret-free render** | SEC-012, C4 | `suggestions-ui.ts` renders only project ref, local time, aggregates, counts, health labels | Builder test `SEC-012 render carries no secret` | PASS |
| `escapeHtml` on all caller text | SEC-012, C4 | All `esc(…)` calls; `actions` buttons use escaped `id` in `data-*` only | Builder test `project names/reasons are escaped` | PASS |
| **No egress** from frontend | DEC-001/017 | `suggestions-ui.ts` is pure string-building; `main.ts` calls only existing SQLite-backed IPC | Code inspection | PASS |

### Cross-cutting guarantees

| Guarantee | Spec ref | Verdict |
|---|---|---|
| Absence ≠ zero — NULL throughout backend; "needs manual time" / "—" in frontend; unmapped surfaced | DEC-004 | PASS |
| No auto-posting — only `accept_time_entry_suggestion` writes `time_entries` | DEC-006 | PASS |
| AI ≠ human — origin tag + separated summary/CSV + separated summaryCards | DEC-003 | PASS |
| Secret-free throughout — SEC-012 builder test + code inspection | SEC-012 | PASS |
| No egress — engine SQLite-only; frontend calls only local IPC | DEC-001/017 | PASS |

---

## Out of scope (confirmed human-only)

- **M1–M3**: packaged macOS app manual acceptance — require running the Tauri app. Not attempted; correctly excluded from automated QA per handoff.md.

## Pre-existing failures (not regressions)

- `tests/pi-observe.security.test.mjs` — 2 tests fail due to network behaviour (401 body-errors path); identical failures on `main` at the time of this gate. Not touched by TASK-032.

---

## Changed / added files (TASK-032 on this branch)

**Backend (Rust):**
- `src-tauri/src/suggestions/mod.rs` (new)
- `src-tauri/src/suggestions/engine.rs` (new)
- `src-tauri/src/suggestions/store.rs` (new)
- `src-tauri/src/suggestions/tests.rs` (new)
- `src-tauri/src/lib.rs` — `time_entries.origin`, 3 IPC commands, `summary_repo`/`export_csv_repo` DEC-003 separation, `accept_suggestion_repo`, `dismiss_suggestion_repo`

**Frontend:**
- `src/suggestions-ui.ts` (new)
- `src/main.ts` — `'Suggestions'` view, `renderSuggestions`, `bindSuggestions`, `summaryCards`, `Summary.ai_minutes`

**Tests:**
- `src-tauri/src/suggestions/tests.rs` (new, A-workstream)
- `src-tauri/src/lib.rs` (B-workstream tests inline)
- `tests/suggestionsUi.test.mjs` (new, C-workstream)

---

**Gate verdict: PASS → route to SW-4 (Code Reviewer) + SW-5 (Security Agent) in parallel.**
