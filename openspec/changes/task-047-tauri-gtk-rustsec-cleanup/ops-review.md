# Ops review — TASK-047 Tauri/GTK RustSec cleanup (SW-2 DevOps)

- **Role**: DevOps (SW-2) · **Date**: 2026-06-21 · **Branch**: `feat/task-047-tauri-gtk-rustsec-cleanup`
- **Decision (fixed by SW-1)**: (c) deferred w/ documented risk acceptance, enforced by a **target-scoped
  `cargo-deny` advisory gate**. See `design.md`. No `Cargo.*`/source/capability/`tauri.conf.json` edit.
- **Tier**: L2 security-posture. CI gate added; no runtime/observability change (no shipped code path).

## 1. What was implemented

| Artifact | Path | Purpose |
|---|---|---|
| cargo-deny config | `src-tauri/deny.toml` | Apple-target-scoped advisory gate + auditable deferred-ID block |
| CI gate | `.github/workflows/dependency-advisories.yml` | runs `cargo deny check advisories` on PR/push/dispatch |
| Contributor docs | `RELEASE.md` (v0.6.3 entry) | local one-liner + posture summary |

- **Mechanism = `[graph].targets`, not an ID ignore list** (per `design.md` §5 / spec). `ignore = []` stays
  empty for the gtk3-rs cluster so the tripwire is real. `unmaintained = "all"` keeps the unmaintained
  cluster armed when a Linux triple is later added.
- **Local one-liner** (documented in `RELEASE.md`): `cd src-tauri && cargo deny check advisories`.

## 2. Tooling note — missing-dependency policy applied

`cargo-deny` and `cargo-audit` are **not installed** in this environment, and the policy is **no install**
(`design.md` §8). The Sonatype guide MCP requires interactive auth (unavailable headless). So the live
binary scan was **not** run locally; instead the deferred IDs were produced by the best available
authoritative method (below). The **real** `cargo deny check advisories` runs in CI, where the action
installs cargo-deny — that is the enforced, repeatable scan going forward.

## 3. Evidence — reachability proof (best-available substitute for the local scanner)

`cargo tree` against the working-tree `src-tauri/Cargo.lock` (cargo 1.95.0):

- **Shipped target** `aarch64-apple-darwin`:
  `cargo tree -i glib --target aarch64-apple-darwin` → **"nothing to print"** (RC 0). The gtk3-rs/glib
  chain is **absent** from the macOS graph.
- **Linux target** `x86_64-unknown-linux-gnu`:
  `cargo tree -i glib --target x86_64-unknown-linux-gnu` → resolves
  `glib ← atk ← gtk ← {muda, tao} ← tauri ← vire` (plus `cairo-rs`/`gdk`/`gdkx11`/`webkit2gtk` branches).

→ The chain is `cfg`-gated to Linux/BSD upstream; runtime reachability on the shipped macOS `.app` is
**zero**. This is the load-bearing fact for option (c) and the basis for the target scoping. It also
demonstrates the tripwire: the Linux triple makes the crates appear in the evaluated graph.

## 4. Deferred advisories (Linux-only backend — NOT shipped)

Generated by cross-referencing the **exact locked versions** in `src-tauri/Cargo.lock` against the RustSec
advisory database (`github.com/rustsec/advisory-db`, shallow clone, 2026-06-21) — i.e. the same data source
`cargo-audit`/`cargo-deny` consume, extracted programmatically (not hand-copied). The complete set of
advisories affecting our locked crates is **12 IDs**:

| Advisory | Crate (locked) | Class | Upstream fix |
|---|---|---|---|
| RUSTSEC-2024-0429 | `glib` 0.18.5 | unsound | `glib ≥ 0.20.0` (gtk4 ecosystem; gtk3 backend cannot consume it) |
| RUSTSEC-2024-0370 | `proc-macro-error` 1.0.4 | unmaintained | none (`manyhow` / `proc-macro2-diagnostics`) |
| RUSTSEC-2024-0411 | `gdkwayland-sys` 0.18.2 | unmaintained | none — gtk3-rs EOL → gtk4-rs |
| RUSTSEC-2024-0412 | `gdk` 0.18.2 | unmaintained | none — gtk3-rs EOL → gtk4-rs |
| RUSTSEC-2024-0413 | `atk` 0.18.2 | unmaintained | none — gtk3-rs EOL → gtk4-rs |
| RUSTSEC-2024-0414 | `gdkx11-sys` 0.18.2 | unmaintained | none — gtk3-rs EOL → gtk4-rs |
| RUSTSEC-2024-0415 | `gtk` 0.18.2 | unmaintained | none — gtk3-rs EOL → gtk4-rs |
| RUSTSEC-2024-0416 | `atk-sys` 0.18.2 | unmaintained | none — gtk3-rs EOL → gtk4-rs |
| RUSTSEC-2024-0417 | `gdkx11` 0.18.2 | unmaintained | none — gtk3-rs EOL → gtk4-rs |
| RUSTSEC-2024-0418 | `gdk-sys` 0.18.2 | unmaintained | none — gtk3-rs EOL → gtk4-rs |
| RUSTSEC-2024-0419 | `gtk3-macros` 0.18.2 | unmaintained | none — gtk3-rs EOL → gtk4-rs |
| RUSTSEC-2024-0420 | `gtk-sys` 0.18.2 | unmaintained | none — gtk3-rs EOL → gtk4-rs |

**Correction to `design.md` §3:** the design referenced the cluster approximately as
"RUSTSEC-2024-0411…0423". The exact range affecting our locked gtk3-rs crates is **…0411–0420** (10 IDs).
The core gtk-rs bindings shared with gtk4 (`cairo-rs`, `pango`, `gdk-pixbuf`, `gio`, `gobject-sys`,
`soup3`, `webkit2gtk`, `javascriptcore-rs` and their `-sys`) are present in the Linux graph but carry **no**
RustSec advisory, so nothing is deferred for them. `glib` (RUSTSEC-2024-0429) is the only scored item.

These IDs are mirrored in the `deny.toml` "Deferred advisories" comment block. SW-5 should record the same
list + risk acceptance in `sec.md` (spec requirement V5 — SW-5-owned artifact, not written here).

## 5. Verification status

| Gate | Status | Note |
|---|---|---|
| V1 scoped check exits 0 on Apple targets | **deferred to CI** | cargo-deny not installable locally; Apple graph is empty of gtk3-rs (§3) so it passes by construction; CI runs the real binary |
| V2 `git diff main -- src-tauri/Cargo.{toml,lock}` empty | **PASS** | verified empty (config/docs only) |
| V3 tripwire re-reports on Linux triple | **PASS (proven via `cargo tree`)** | Linux target resurfaces `glib`+cluster in the graph (§3); the scoped→Linux delta is the same input cargo-deny evaluates |
| V4 `cargo test` still green | **N/A by construction** | no source/dep change; deny.toml/workflow are not compiled |
| V5 `sec.md` records deferred IDs | **handoff to SW-5** | all inputs provided here + in `deny.toml` |

> V1/V3 are fully exercised by the CI gate's first run (real cargo-deny). Locally they are evidenced by the
> `cargo tree` reachability delta, which is the exact dependency-graph signal cargo-deny advisories consume.

## 6. CI / DevOps notes

- Runner: **ubuntu-latest**. The advisory check is graph-only and **target-scoped** via `deny.toml`, so it
  evaluates the Apple-target graph regardless of runner OS — ubuntu is chosen purely for speed/cost. The
  gate's correctness comes from `[graph].targets`, not the runner. Documented in the workflow header.
- `permissions: contents: read` (least privilege). Triggers: `pull_request`, `push` to `main`,
  `workflow_dispatch`. No secrets used.
- Actions pinned to major versions: `actions/checkout@v4`, `dtolnay/rust-toolchain@stable`,
  `taiki-e/install-action@v2` (installs cargo-deny). A future hardening task may pin to commit SHAs.
- Scope held: advisories-only. `[bans]`/`[licenses]` left permissive (out of TASK-047 scope) so the
  advisory gate runs standalone without a license inventory.

## 7. Handoffs

- → **SW-5 (security)**: write `sec.md` from §4 (deferred IDs + reachability proof + risk acceptance +
  review trigger). All inputs are here and in `deny.toml`.
- → **SW-3 (QA)**: re-enter per the tripwire if a Linux build target is added or Tauri's Linux backend
  migrates off GTK3 (then `glib ≥ 0.20` becomes in-scope → real bump + packaged-app QA).
- → **SW-6 (release)**: `RELEASE.md` v0.6.3 entry drafted; finalize version number at release time.
